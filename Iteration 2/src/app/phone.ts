import { SafeStyle } from '@angular/platform-browser';

export class Phone {
    id: number;
    img: string;
    title: string;
    tagline: string;
    price: string;
    condition: string;
    capacity: string;
    color: SafeStyle;
}
