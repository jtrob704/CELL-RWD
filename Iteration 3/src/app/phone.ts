import { SafeStyle } from '@angular/platform-browser';

export class Phone {
    id: number;
    img: string;
    title: string;
    tagline: string;
    price: number;
    condition: string;
    capacity: string;
    color: SafeStyle;
    quantity: number;
}
